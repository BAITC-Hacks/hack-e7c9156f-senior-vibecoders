server {
    listen ${PORT};
    client_max_body_size 100m;
    root /usr/share/nginx/html;
    location /api/ { proxy_pass http://127.0.0.1:8000; proxy_read_timeout 600s; }
    location = /health { proxy_pass http://127.0.0.1:8000; }
    location / { try_files $uri /index.html; }
}
